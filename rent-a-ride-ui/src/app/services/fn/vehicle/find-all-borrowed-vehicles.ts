/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageResponseBorrowedVehicleResponse } from '../../models/page-response-borrowed-vehicle-response';

export interface FindAllBorrowedVehicles$Params {
  page?: number;
  size?: number;
}

export function findAllBorrowedVehicles(http: HttpClient, rootUrl: string, params?: FindAllBorrowedVehicles$Params, context?: HttpContext): Observable<StrictHttpResponse<PageResponseBorrowedVehicleResponse>> {
  const rb = new RequestBuilder(rootUrl, findAllBorrowedVehicles.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageResponseBorrowedVehicleResponse>;
    })
  );
}

findAllBorrowedVehicles.PATH = '/vehicles/borrowed';
